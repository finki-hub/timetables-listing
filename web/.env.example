# PostHog product analytics (public, write-only browser ingest key).
# A working default is baked into the app, so these are optional overrides.
VITE_POSTHOG_KEY=phc_xXEqLMnYeDPuXA6HHwuasQMdSufDGryS8vZZuHmu9Qwd
VITE_POSTHOG_HOST=https://eu.i.posthog.com

# Timetables API base URL (optional; defaults to the production endpoint).
# VITE_TIMETABLES_API_URL=https://timetables-api.finki-hub.com
